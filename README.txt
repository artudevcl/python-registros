Como iniciar el proyecto

1. Clonar proyecto y luego ir a la raiz del proyecto.

2. Configurar el .env (Dejar en la raiz del proyecto).   Adjunto la estructura por default que se usó.

3. Tener docker arriba y Ejecutar docker-compose up -d --build

 con eso esperamos levantar PostgreSQL y el servicio Api.

4. Servicio/bd arriba en puertos y direcciones por defecto. http://localhost:8000